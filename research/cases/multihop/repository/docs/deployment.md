# Deployment

The Kubernetes runtime is provided. Install dependencies with
[`install-dependencies.sh`](../scripts/install-dependencies.sh), then install the Cart service with
[`install-cart.sh`](../scripts/install-cart.sh). Validate the service with
[`validate-cart.sh`](../scripts/validate-cart.sh).
